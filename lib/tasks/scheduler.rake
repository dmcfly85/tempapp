#desc "This task is called by the Heroku scheduler add-on"
#task :get_temps => :environment do
#  puts "Getting temps and creating db entry..."
  #t = Temp.new
  #t.insidetemp = Temp.gettemp_internal
  #t.outsidetemp = Temp.gettemp_external
  #t.save
#  puts "done."
#end
